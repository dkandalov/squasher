name: CI

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: macos-10.15
    steps:
    - uses: actions/checkout@v2
    - name: Install nasm
      run: brew install nasm
    - name: Make
      run: make
    - name: Test
      run: make test
